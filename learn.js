// some days my learn program with for loop
var myLearn = ["vairable", "Some-conditin", "Operation", "Shorthand", "Conversion", "Array", "Loop"];
for (var i = 0; i < myLearn.length; i++) {
    var element = myLearn[i];
    console.log(element);
}

// while loop
var myLearn = ["vairable", "Some-conditin", "Operation", "Shorthand", "Conversion", "Array", "Loop"];
var i = 0;
while (i < myLearn.length) {
    var element = myLearn[i];
    i++;
    console.log(element);
}